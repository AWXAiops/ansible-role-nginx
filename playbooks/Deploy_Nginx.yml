---
- hosts: localhost
  gather_facts: no
  tasks:
    - name: install nginx role
      command: "ansible-galaxy install nginxinc.nginx"
      register: outpt
    - debug: var=outpt

- hosts: all
  gather_facts: no
  tasks:
    - name: Include the example role
      include_role:
        name: nginxinc.nginx
